# FreshFace iOS 应用优化计划 V2.0

> **计划版本**: 2.0
> **创建日期**: 2026-04-07
> **目标市场**: 美国 (iOS 17+)
> **应用 Bundle ID**: com.zzoutuo.freshface
> **架构模式**: MVVM + Clean Architecture

---

## 📋 **目录**

1. [修订说明](#修订说明)
2. [问题分析](#问题分析)
3. [优化优先级矩阵](#优化优先级矩阵)
4. [P0 阻塞发布问题 (必须修复)](#p0-阻塞发布问题-必须修复)
5. [P1 高优先级优化](#p1-高优先级优化)
6. [设计原则与约束](#设计原则与约束)
7. [检验标准与测试用例](#检验标准与测试用例)
8. [文件结构规划](#文件结构规划)

---

## 修订说明

### 背景

基于对软件流程的深入分析，发现以下问题需要优化：

| 问题 | 影响 | 优先级 |
|------|------|--------|
| **Routine 数据未连接 CloudKit** | Routine 数据仅本地存储，无法跨设备同步 | P0 |
| **RoutineDetailView 未集成 RoutineStep** | 新建的 RoutineStep 功能无法使用 | P0 |
| **AddProductView 未集成 BarcodeScanner sheet** | 条码扫描功能无法使用 | P0 |
| **Mock 数据切换问题** | 部分 View 使用 MockRepository，部分使用 CloudRepository | P1 |
| **RoutineRepository 未实现** | 无法进行 Routine 的 CRUD 操作 | P1 |

### ⚠️ V2.1 修复说明

| 问题编号 | 原问题 | 修复方案 |
|---------|--------|----------|
| O1 | 计划称 Routine.swift "已有 steps 字段" | **错误** - 实际 Routine.swift **没有** steps 字段，需在 O1 中添加 |
| O2 | 计划称 RoutineDetailView 使用 @State var routine | **不完整** - 实际是 let routine: Routine，需改为 @State var routine |
| O2 | 计划称 steps 数据来自 routine.steps | **错误** - 实际是 @State private var steps: [String] = [] |
| O3 | 计划称 AddProductView "已有 barcodeSection" | **错误** - 实际 AddProductView **没有** barcodeSection，需新增 |
| O3 | 计划称 AddProductView 使用 CloudProductRepository | **错误** - 实际 AddProductView 接收外部注入的 repository |
| O4 | 计划称 AddProductView 使用 CloudProductRepository | **错误** - 实际 AddProductView 通过 init 参数注入 |

---

## 问题分析

### 1. Repository 实现状态

```
ProductRepositoryProtocol
├── fetchAll()         ✅ CloudProductRepository 已实现
├── fetch(byId)        ✅ CloudProductRepository 已实现
├── create()          ✅ CloudProductRepository 已实现
├── update()          ✅ CloudProductRepository 已实现
├── delete()          ✅ CloudProductRepository 已实现
├── fetchExpiring()    ✅ CloudProductRepository 已实现
├── search()          ✅ CloudProductRepository 已实现
└── filter()          ✅ CloudProductRepository 已实现

RoutineRepositoryProtocol
├── fetchAll()         ⚠️ Mock (需实现 CloudKit 版本)
├── fetch(byId)        ⚠️ Mock
├── create()          ⚠️ Mock
├── update()          ⚠️ Mock
└── delete()          ⚠️ Mock

UsageLogRepositoryProtocol
├── fetch()            ⚠️ Mock (未实现)
├── create()          ⚠️ Mock (未实现)
└── delete()          ⚠️ Mock (未实现)
```

### 2. View 与 Repository 绑定问题

| View | 当前使用 | 问题 |
|------|---------|------|
| HomeView | MockProductRepository | ⚠️ 应使用 CloudProductRepository |
| ProductListView | MockProductRepository | ⚠️ 应使用 CloudProductRepository |
| AnalyticsView | MockProductRepository | ⚠️ 应使用 CloudProductRepository |
| AddProductView | CloudProductRepository | ✅ 正确 |
| RoutineListView | 本地 Mock 数组 | ⚠️ 应使用 RoutineRepository |
| RoutineDetailView | 本地 Mock | ⚠️ 应使用 RoutineRepository |

---

## 优化优先级矩阵

### 优先级定义

| 优先级 | 定义 | 时间要求 |
|--------|------|---------|
| **P0** | 阻塞发布 | 立即修复 |
| **P1** | 高优先级 | 1-2天优化 |

### 模块总览

```
P0 模块 (3个):
├── O1: RoutineRepository CloudKit 实现
├── O2: RoutineDetailView 集成 RoutineStep
└── O3: AddProductView 集成 BarcodeScanner

P1 模块 (2个):
├── O4: 统一 Repository 注入 (统一使用 CloudProductRepository)
└── O5: UsageLogRepository 实现
```

---

## P0 阻塞发布问题 (必须修复)

### O1: RoutineRepository CloudKit 实现

#### O1.1 需求描述

```markdown
## 需求描述
- 实现 RoutineRepositoryProtocol 的 CloudKit 版本
- 支持 Routine 数据的 CRUD 操作
- 支持跨设备同步
- Routine 数据结构需要包含 steps 数组

## 用户场景
1. 用户创建 Morning Routine → 数据保存到 iCloud
2. 用户在 iPad 上查看 Routine → 数据从 iCloud 同步
3. 用户删除 Routine → iCloud 数据同步删除
```

#### O1.2 与现有代码集成

```swift
// ========== 关键集成点 ==========

// 1. Domain/Entities/Routine.swift - 已有结构，需【修改】添加 steps 字段
struct Routine: Identifiable, Codable, Hashable {
    let id: UUID
    var name: String
    var routineType: RoutineType
    var time: Date?
    var steps: [RoutineStep]  // ⚠️ 【新增】原结构无此字段，需添加
    var isActive: Bool
    let createdAt: Date
    var updatedAt: Date

    init(
        id: UUID = UUID(),
        name: String,
        routineType: RoutineType,
        time: Date? = nil,
        steps: [RoutineStep] = [],  // ⚠️ 【新增】init 参数
        isActive: Bool = true,
        createdAt: Date = Date(),
        updatedAt: Date = Date()
    ) {
        self.id = id
        self.name = name
        self.routineType = routineType
        self.time = time
        self.steps = steps  // ⚠️ 【新增】
        self.isActive = isActive
        self.createdAt = createdAt
        self.updatedAt = updatedAt
    }
}

// 2. Data/Repositories/ - 需要新增 CloudRoutineRepository
class CloudRoutineRepository: RoutineRepositoryProtocol {
    private let database: CKDatabase
    private let recordType = "Routine"

    // 实现 CRUD 方法
}

// 3. RoutineListView - 需要注入 Repository
struct RoutineListView: View {
    @StateObject private var viewModel: RoutineListViewModel

    init() {
        _viewModel = StateObject(wrappedValue: RoutineListViewModel(
            repository: CloudRoutineRepository()  // 修改为 CloudKit
        ))
    }
}
```

#### O1.3 文件结构

```
Data/
└── Repositories/
    ├── MockRepositories.swift      # 已存在
    ├── CloudProductRepository.swift # 已存在
    └── CloudRoutineRepository.swift # 新增 (O1)
```

#### O1.4 代码示例

```swift
// CloudRoutineRepository.swift
import Foundation
import CloudKit

class CloudRoutineRepository: RoutineRepositoryProtocol {

    private let database: CKDatabase
    private let recordType = "Routine"

    init(database: CKDatabase = CloudKitManager.shared.privateDatabase) {
        self.database = database
    }

    func fetchAll() async throws -> [Routine] {
        let query = CKQuery(recordType: recordType, predicate: NSPredicate(value: true))
        query.sortDescriptors = [NSSortDescriptor(key: "updatedAt", ascending: false)]

        let (matchResults, _) = try await database.records(matching: query)

        var routines: [Routine] = []
        for (_, result) in matchResults {
            switch result {
            case .success(let record):
                if let routine = routineFromRecord(record) {
                    routines.append(routine)
                }
            case .failure(let error):
                print("Error fetching routine: \(error)")
            }
        }
        return routines
    }

    func fetch(byId id: UUID) async throws -> Routine? {
        let recordID = CKRecord.ID(recordName: id.uuidString)
        do {
            let record = try await database.record(for: recordID)
            return routineFromRecord(record)
        } catch {
            return nil
        }
    }

    func create(_ routine: Routine) async throws -> Routine {
        let record = recordFromRoutine(routine)
        let savedRecord = try await database.save(record)
        return routineFromRecord(savedRecord) ?? routine
    }

    func update(_ routine: Routine) async throws -> Routine {
        let recordID = CKRecord.ID(recordName: routine.id.uuidString)
        do {
            let existingRecord = try await database.record(for: recordID)
            let updatedRecord = updateRecord(existingRecord, with: routine)
            let savedRecord = try await database.save(updatedRecord)
            return routineFromRecord(savedRecord) ?? routine
        } catch {
            throw error
        }
    }

    func delete(_ routine: Routine) async throws {
        let recordID = CKRecord.ID(recordName: routine.id.uuidString)
        try await database.deleteRecord(withID: recordID)
    }

    private func recordFromRoutine(_ routine: Routine) -> CKRecord {
        let record = CKRecord(recordType: recordType, recordID: CKRecord.ID(recordName: routine.id.uuidString))

        record["name"] = routine.name
        record["routineType"] = routine.routineType.rawValue
        record["time"] = routine.time
        record["isActive"] = routine.isActive
        record["createdAt"] = routine.createdAt
        record["updatedAt"] = Date()

        if let stepsData = try? JSONEncoder().encode(routine.steps) {
            record["stepsData"] = stepsData
        }

        return record
    }

    private func routineFromRecord(_ record: CKRecord) -> Routine? {
        guard let name = record["name"] as? String,
              let routineTypeString = record["routineType"] as? String,
              let routineType = RoutineType(rawValue: routineTypeString),
              let isActive = record["isActive"] as? Bool,
              let createdAt = record["createdAt"] as? Date else {
            return nil
        }

        var steps: [RoutineStep] = []
        if let stepsData = record["stepsData"] as? Data {
            steps = (try? JSONDecoder().decode([RoutineStep].self, from: stepsData)) ?? []
        }

        return Routine(
            id: UUID(uuidString: record.recordID.recordName) ?? UUID(),
            name: name,
            routineType: routineType,
            time: record["time"] as? Date,
            steps: steps,
            isActive: isActive,
            createdAt: createdAt,
            updatedAt: record["updatedAt"] as? Date ?? Date()
        )
    }

    private func updateRecord(_ record: CKRecord, with routine: Routine) -> CKRecord {
        record["name"] = routine.name
        record["routineType"] = routine.routineType.rawValue
        record["time"] = routine.time
        record["isActive"] = routine.isActive
        record["updatedAt"] = Date()

        if let stepsData = try? JSONEncoder().encode(routine.steps) {
            record["stepsData"] = stepsData
        }

        return record
    }
}
```

#### O1.5 检验标准

```swift
// 检验标准 Checklist:
// [ ] CloudRoutineRepository 实现 ProductRepositoryProtocol
// [ ] CRUD 操作正确保存到 CloudKit
// [ ] Routine.steps 正确序列化和反序列化
// [ ] RoutineListView 使用 CloudRoutineRepository
// [ ] 跨设备同步正常工作
// [ ] 错误处理完善
```

---

### O2: RoutineDetailView 集成 RoutineStep

#### O2.1 需求描述

```markdown
## 需求描述
- 在 RoutineDetailView 中显示步骤列表
- 支持添加、编辑、删除步骤
- 支持拖拽排序步骤
- 支持步骤完成打卡

## 用户场景
1. 用户查看 Routine 详情 → 显示步骤列表
2. 用户点击 "Add Step" → 显示 RoutineStepEditorView
3. 用户拖拽调整步骤顺序 → 自动保存
4. 用户点击步骤完成 → 打卡记录
```

#### O2.2 与现有代码集成

```swift
// ========== 关键集成点 ==========

// 1. 实际现有 RoutineDetailView.swift 代码:
struct RoutineDetailView: View {
    let routine: Routine  // ⚠️ 【问题】let 不可修改，需改为 @State var
    @State private var steps: [String] = []  // ⚠️ 【问题】现有是 [String]，需改为 [RoutineStep]
    // ...
}

// 2. 需要修改为:
struct RoutineDetailView: View {
    @State var routine: Routine  // ⚠️ 【修改】改为 @State var 以支持修改
    @State private var showAddStep = false  // 新增
    @State private var editingStep: RoutineStep? = nil  // 新增

    // ... 添加步骤编辑功能
}

// 3. 关键修改点:
// - @State var routine: Routine (let → @State var)
// - @State private var steps: [String] → @State private var steps: [RoutineStep]
// - 添加 sheet 绑定 showAddStep
// - 添加 sheet 绑定 editingStep
// - 添加步骤编辑 UI
```

#### O2.3 代码示例

```swift
// RoutineDetailView.swift (增强版)
import SwiftUI

struct RoutineDetailView: View {
    @State var routine: Routine
    @State private var showAddStep = false
    @State private var editingStep: RoutineStep?

    var body: some View {
        List {
            infoSection

            stepsSection

            addStepButton
        }
        .navigationTitle(routine.name)
        .navigationBarTitleDisplayMode(.large)
        .sheet(isPresented: $showAddStep) {
            RoutineStepEditorView(
                step: nil,
                availableProducts: []
            ) { newStep in
                addStep(newStep)
            }
        }
        .sheet(item: $editingStep) { step in
            RoutineStepEditorView(
                step: step,
                availableProducts: []
            ) { updatedStep in
                updateStep(updatedStep)
            }
        }
    }

    private var infoSection: some View {
        Section {
            HStack {
                Image(systemName: routine.routineType.icon)
                    .foregroundColor(Color(hex: "FF6B9D"))
                Text(routine.routineType.displayName)

                Spacer()

                if routine.isActive {
                    Label("Active", systemImage: "checkmark.circle.fill")
                        .font(.caption)
                        .foregroundColor(.green)
                }
            }

            if let time = routine.time {
                HStack {
                    Image(systemName: "clock")
                    Text(time.formatted(date: .omitted, time: .shortened))
                }
            }
        }
    }

    private var stepsSection: some View {
        Section(header: Text("Steps (\(routine.steps.count))")) {
            if routine.steps.isEmpty {
                Text("No steps yet. Add your first step to get started.")
                    .font(.subheadline)
                    .foregroundColor(.secondary)
                    .listRowBackground(Color.clear)
            } else {
                ForEach(Array(routine.steps.enumerated()), id: \.element.id) { index, step in
                    RoutineStepRowView(
                        step: step,
                        stepNumber: index + 1,
                        onToggleComplete: { toggleStep(step) },
                        onDelete: { deleteStep(step) }
                    )
                    .contentShape(Rectangle())
                    .onTapGesture {
                        editingStep = step
                    }
                }
                .onMove(perform: moveSteps)
            }
        }
    }

    private var addStepButton: some View {
        Section {
            Button(action: { showAddStep = true }) {
                Label("Add Step", systemImage: "plus.circle.fill")
            }
        }
    }

    private func addStep(_ step: RoutineStep) {
        var newStep = step
        newStep.stepOrder = routine.steps.count
        routine.steps.append(newStep)
        saveRoutine()
    }

    private func updateStep(_ step: RoutineStep) {
        if let index = routine.steps.firstIndex(where: { $0.id == step.id }) {
            routine.steps[index] = step
            saveRoutine()
        }
    }

    private func deleteStep(_ step: RoutineStep) {
        routine.steps.removeAll { $0.id == step.id }
        reorderSteps()
        saveRoutine()
    }

    private func toggleStep(_ step: RoutineStep) {
        if let index = routine.steps.firstIndex(where: { $0.id == step.id }) {
            routine.steps[index].isCompleted.toggle()
            saveRoutine()
        }
    }

    private func moveSteps(from source: IndexSet, to destination: Int) {
        routine.steps.move(fromOffsets: source, toOffset: destination)
        reorderSteps()
        saveRoutine()
    }

    private func reorderSteps() {
        for (index, _) in routine.steps.enumerated() {
            routine.steps[index].stepOrder = index
        }
    }

    private func saveRoutine() {
        Task {
            do {
                try await CloudRoutineRepository().update(routine)
            } catch {
                print("Error saving routine: \(error)")
            }
        }
    }
}
```

#### O2.4 检验标准

```swift
// 检验标准 Checklist:
// [ ] 步骤列表正确显示
// [ ] 可以添加新步骤
// [ ] 可以编辑现有步骤
// [ ] 可以删除步骤
// [ ] 可以拖拽排序
// [ ] 点击步骤切换完成状态
// [ ] 数据正确保存到 CloudKit
```

---

### O3: AddProductView 集成 BarcodeScanner

#### O3.1 需求描述

```markdown
## 需求描述
- 在 AddProductView 中添加扫码按钮
- 点击扫码按钮显示 BarcodeScannerView
- 扫描成功后自动填充 barcode 字段
- 支持手动输入条码

## 用户场景
1. 用户点击 "Scan Barcode" → 显示相机扫描界面
2. 扫描成功 → 自动填充 barcode 字段，关闭扫描界面
3. 用户可以手动编辑条码
```

#### O3.2 与现有代码集成

```swift
// ========== 关键集成点 ==========

// 1. AddProductView.swift 实际结构:
struct AddProductView: View {
    @StateObject private var viewModel: AddProductViewModel
    @Environment(\.dismiss) var dismiss

    init(repository: ProductRepositoryProtocol) {  // ⚠️ 【注意】通过参数注入
        _viewModel = StateObject(wrappedValue: AddProductViewModel(repository: repository))
    }

    var body: some View {
        Form {
            imageSection      // 已有
            basicInfoSection  // 已有
            dateAndPAOSection // 已有
            additionalInfoSection  // 已有
            // ⚠️ 【缺失】barcodeSection 需要新增
        }
    }

    // imageSection 中已有:
    // Button(action: { viewModel.showScanner = true }) { Label("Scan Barcode", ...) }
    // 但没有 .sheet(isPresented: $viewModel.showScanner) 来显示扫描界面
}

// 2. 关键修改点:
// - 在 Form 中添加 barcodeSection (包含 TextField 和扫码按钮)
// - 添加 .sheet(isPresented: $viewModel.showScanner)

// 3. BarcodeScannerView 已存在于:
// Presentation/Views/Product/BarcodeScannerView.swift
```

#### O3.3 代码示例

```swift
// AddProductView.swift 修改 - 在 Form 中添加 barcodeSection 并添加 sheet
struct AddProductView: View {
    @StateObject private var viewModel: AddProductViewModel
    @Environment(\.dismiss) var dismiss

    init(repository: ProductRepositoryProtocol) {
        _viewModel = StateObject(wrappedValue: AddProductViewModel(repository: repository))
    }

    var body: some View {
        NavigationStack {
            Form {
                imageSection

                basicInfoSection

                dateAndPAOSection

                additionalInfoSection

                // ⚠️ 【新增】barcodeSection
                barcodeSection
            }
            // ⚠️ 【新增】sheet 用于显示 BarcodeScannerView
            .sheet(isPresented: $viewModel.showScanner) {
                BarcodeScannerView { barcode in
                    viewModel.barcode = barcode
                }
            }
            // ... 其他代码
        }
    }

    // ⚠️ 【新增】barcodeSection
    private var barcodeSection: some View {
        Section(header: Text("Barcode (Optional)")) {
            HStack {
                TextField("Enter barcode", text: Binding(
                    get: { viewModel.barcode ?? "" },
                    set: { viewModel.barcode = $0.isEmpty ? nil : $0 }
                ))
                .keyboardType(.numberPad)

                Button(action: { viewModel.showScanner = true }) {
                    Image(systemName: "viewfinder")
                        .font(.title2)
                        .foregroundColor(Color(hex: "FF6B9D"))
                }
            }
        }
    }
}
```

#### O3.4 检验标准

```swift
// 检验标准 Checklist:
// [ ] 扫码按钮正确显示
// [ ] 点击显示 BarcodeScannerView
// [ ] 扫描成功自动填充 barcode
// [ ] 支持手动输入条码
// [ ] 相机权限正确请求
```

---

## P1 高优先级优化

### O4: 统一 Repository 注入

#### O4.1 需求描述

```markdown
## 需求描述
- 统一所有 View 使用 CloudProductRepository
- 移除 MockProductRepository 的直接使用
- 保持架构一致性

## 修改范围
| View | 修改前 | 修改后 |
|------|--------|--------|
| HomeView | MockProductRepository | CloudProductRepository |
| ProductListView | MockProductRepository | CloudProductRepository |
| AnalyticsView | MockProductRepository | CloudProductRepository |
```

#### O4.2 检验标准

```swift
// 检验标准 Checklist:
// [ ] HomeView 使用 CloudProductRepository
// [ ] ProductListView 使用 CloudProductRepository
// [ ] AnalyticsView 使用 CloudProductRepository
// [ ] AddProductView 继续使用 CloudProductRepository
// [ ] 所有产品数据从 CloudKit 加载
```

---

### O5: UsageLogRepository 实现

#### O5.1 需求描述

```markdown
## 需求描述
- 实现 UsageLogRepositoryProtocol
- 支持记录产品使用历史
- 支持产品使用统计

## 数据结构
struct UsageLog: Identifiable, Codable {
    let id: UUID
    let productId: UUID
    let usedAt: Date
    let quantity: Int?
}
```

#### O5.2 代码示例

```swift
// CloudUsageLogRepository.swift
import Foundation
import CloudKit

class CloudUsageLogRepository: UsageLogRepositoryProtocol {

    private let database: CKDatabase
    private let recordType = "UsageLog"

    init(database: CKDatabase = CloudKitManager.shared.privateDatabase) {
        self.database = database
    }

    func fetch(for productId: UUID) async throws -> [UsageLog] {
        let predicate = NSPredicate(format: "productId == %@", productId.uuidString)
        let query = CKQuery(recordType: recordType, predicate: predicate)
        query.sortDescriptors = [NSSortDescriptor(key: "usedAt", ascending: false)]

        let (matchResults, _) = try await database.records(matching: query)

        var logs: [UsageLog] = []
        for (_, result) in matchResults {
            if case .success(let record) = result,
               let log = usageLogFromRecord(record) {
                logs.append(log)
            }
        }
        return logs
    }

    func create(_ log: UsageLog) async throws -> UsageLog {
        let record = recordFromUsageLog(log)
        let savedRecord = try await database.save(record)
        return usageLogFromRecord(savedRecord) ?? log
    }

    func delete(_ log: UsageLog) async throws {
        let recordID = CKRecord.ID(recordName: log.id.uuidString)
        try await database.deleteRecord(withID: recordID)
    }

    private func recordFromUsageLog(_ log: UsageLog) -> CKRecord {
        let record = CKRecord(recordType: recordType, recordID: CKRecord.ID(recordName: log.id.uuidString))
        record["productId"] = log.productId.uuidString
        record["usedAt"] = log.usedAt
        record["quantity"] = log.quantity
        return record
    }

    private func usageLogFromRecord(_ record: CKRecord) -> UsageLog? {
        guard let productIdString = record["productId"] as? String,
              let productId = UUID(uuidString: productIdString),
              let usedAt = record["usedAt"] as? Date else {
            return nil
        }

        return UsageLog(
            id: UUID(uuidString: record.recordID.recordName) ?? UUID(),
            productId: productId,
            usedAt: usedAt,
            quantity: record["quantity"] as? Int
        )
    }
}
```

---

## 设计原则与约束

### 功能模块原则

```
✅ 单一职责: 每个文件只负责一个功能
✅ 高内聚: 相关功能放同一模块
✅ 低耦合: 模块间通过协议通信
✅ 命名清晰: 文件名 = 功能名
```

### 代码复用原则 (三次法则)

```
第1次出现: 直接写
第2次出现: 复制粘贴 (可接受)
第3次出现: 抽象提取为通用组件/方法

✅ 已复用:
- CloudKitManager (iCloud 管理)
- CloudProductRepository (CRUD 模板)
- RoutineStep 实体
- BarcodeScannerService
```

### 废弃代码清理流程

```
1. 标记废弃: @available(*, deprecated, message: "...")
2. 验证无影响: 确认无调用点
3. 删除代码: 移除废弃代码
4. 提交信息: "refactor: remove deprecated XXX"
```

### 继承与兼容性原则

```swift
// ✅ 正确: 复用已有协议和基类
protocol RoutineRepositoryProtocol { ... }

// ✅ 正确: 依赖注入
class RoutineListViewModel {
    init(repository: RoutineRepositoryProtocol) {
        self.repository = repository
    }
}

// ❌ 错误: 硬编码具体实现
class RoutineListViewModel {
    private let repository = MockRoutineRepository()  // 硬编码
}
```

---

## 检验标准与测试用例

### 模块检验标准模板

```swift
// MARK: - 模块检验标准
/*
模块名称: [Module Name]
完成日期: [Date]

✅ 检验清单:
[ ] 代码符合 Swift 代码规范
[ ] 遵循 MVVM + Clean Architecture
[ ] 所有协议通过依赖注入
[ ] 无硬编码字符串
[ ] 支持 Dark Mode
[ ] 支持 Dynamic Type
[ ] UI 预览正常
[ ] 错误处理完善
[ ] 与现有代码集成正确
[ ] 数据正确保存到 CloudKit
*/
```

### 集成测试用例

```swift
// Test Case 1: Routine CRUD
func testRoutine_CRUD() async {
    // Given: CloudRoutineRepository
    let repository = CloudRoutineRepository()

    // When: Create routine
    let routine = Routine(name: "Test Routine", routineType: .morning)
    let created = try await repository.create(routine)

    // Then: Fetch and verify
    let fetched = try await repository.fetch(byId: created.id)
    XCTAssertEqual(fetched?.name, "Test Routine")

    // When: Update routine
    var updated = fetched!
    updated.name = "Updated Name"
    try await repository.update(updated)

    // Then: Verify update
    let reFetched = try await repository.fetch(byId: updated.id)
    XCTAssertEqual(reFetched?.name, "Updated Name")

    // When: Delete routine
    try await repository.delete(updated)

    // Then: Verify deletion
    let deleted = try await repository.fetch(byId: updated.id)
    XCTAssertNil(deleted)
}

// Test Case 2: Routine Steps
func testRoutine_Steps() async {
    // Given: Routine with steps
    let step1 = RoutineStep(productName: "Step 1", stepOrder: 0)
    let step2 = RoutineStep(productName: "Step 2", stepOrder: 1)
    let routine = Routine(
        name: "Morning Routine",
        routineType: .morning,
        steps: [step1, step2]
    )

    // When: Save and fetch
    let repository = CloudRoutineRepository()
    let created = try await repository.create(routine)
    let fetched = try await repository.fetch(byId: created.id)

    // Then: Verify steps
    XCTAssertEqual(fetched?.steps.count, 2)
    XCTAssertEqual(fetched?.steps[0].productName, "Step 1")
    XCTAssertEqual(fetched?.steps[1].productName, "Step 2")
}

// Test Case 3: Barcode Scanner Integration
func testAddProductView_BarcodeScanner() {
    // Given: AddProductView with barcode scanner
    let view = AddProductView(repository: CloudProductRepository())

    // When: Tap scan button
    view.viewModel.showScanner = true

    // Then: Scanner sheet appears
    // Simulate scan with barcode "1234567890123"
    view.viewModel.barcode = "1234567890123"

    // Verify barcode is set
    XCTAssertEqual(view.viewModel.barcode, "1234567890123")
}
```

---

## 文件结构规划

### 新增文件

| 文件 | 优先级 | 说明 |
|------|--------|------|
| CloudRoutineRepository.swift | P0 | Routine CloudKit CRUD 实现 |
| CloudUsageLogRepository.swift | P1 | UsageLog CloudKit CRUD 实现 |

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| Routine.swift | ⚠️ 添加 steps: [RoutineStep] 字段和 init 参数 |
| RoutineDetailView.swift | ⚠️ let routine → @State var routine，steps: [String] → [RoutineStep]，添加步骤编辑功能 |
| AddProductView.swift | ⚠️ 添加 barcodeSection，添加 .sheet(isPresented:) 绑定 BarcodeScannerView |
| HomeView.swift | ✅ 使用 CloudProductRepository |
| ProductListView.swift | ✅ 使用 CloudProductRepository |
| AnalyticsView.swift | ✅ 使用 CloudProductRepository |
| AddRoutineView.swift | ✅ V2.2 新增 - 添加 `onSave: (Routine) -> Void` 闭包参数 |
| RoutineListView.swift | ✅ V2.2 新增 - 改用 CloudRoutineRepository，实现 loadRoutines(), addRoutine(), deleteRoutine() |

### ⚠️ 文件路径参考 (已验证)

```
freshface/
├── Domain/
│   └── Entities/
│       ├── Routine.swift          # ⚠️ 需添加 steps 字段
│       └── RoutineStep.swift      # ✅ 已存在
├── Data/
│   └── Repositories/
│       ├── MockRepositories.swift  # ✅ 已存在 (MockProductRepository, MockRoutineRepository)
│       ├── CloudProductRepository.swift # ✅ 已存在
│       └── CloudRoutineRepository.swift # ⚠️ 需新增
├── Presentation/
│   └── Views/
│       ├── Product/
│       │   ├── AddProductView.swift      # ⚠️ 需添加 barcodeSection
│       │   └── BarcodeScannerView.swift  # ✅ 已存在
│       └── Routine/
│           ├── RoutineListView.swift     # ⚠️ 需使用 RoutineRepository
│           └── RoutineDetailView.swift   # ⚠️ 需大改
```

---

## V2.2 实施记录

### 修复日期
2026-04-07

### 新增修复问题

| 问题编号 | 问题描述 | 修复方案 | 状态 |
|---------|---------|---------|------|
| R1 | AddRoutineView 没有保存回调 | 添加 `onSave: (Routine) -> Void` 闭包参数 | ✅ 已完成 |
| R2 | RoutineListView 使用本地 Mock | 改用 CloudRoutineRepository，实现 CRUD 操作 | ✅ 已完成 |

### 修改文件清单

| 文件 | 修改内容 |
|------|---------|
| AddRoutineView.swift | 添加 `let onSave: (Routine) -> Void` 闭包，点击 Save 时调用 |
| RoutineListView.swift | 改用 CloudRoutineRepository，实现 loadRoutines(), addRoutine(), deleteRoutine() |

### Repository 实现状态 (更新后)

```
ProductRepositoryProtocol
├── fetchAll()         ✅ CloudProductRepository
├── fetch(byId)        ✅ CloudProductRepository
├── create()          ✅ CloudProductRepository
├── update()          ✅ CloudProductRepository
├── delete()          ✅ CloudProductRepository
├── fetchExpiring()    ✅ CloudProductRepository
├── search()          ✅ CloudProductRepository
└── filter()          ✅ CloudProductRepository

RoutineRepositoryProtocol
├── fetchAll()         ✅ CloudRoutineRepository
├── fetch(byId)        ✅ CloudRoutineRepository
├── create()          ✅ CloudRoutineRepository
├── update()          ✅ CloudRoutineRepository
└── delete()          ✅ CloudRoutineRepository

UsageLogRepositoryProtocol
├── fetch()            ✅ CloudUsageLogRepository
├── create()          ✅ CloudUsageLogRepository
└── delete()          ✅ CloudUsageLogRepository
```

---

**计划版本**: 2.2 (实施版)
**计划完成日期**: 2026-04-07
**实施状态**: ✅ 全部完成
